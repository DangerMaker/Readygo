<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="10dp"
    android:id="@+id/toolbar"
    android:background="@drawable/youku_toolbar_bg"
    android:paddingTop="5dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/searchBtn"
            style="@style/toolbar_menu_style"
            android:layout_alignParentLeft="true"
            android:src="@drawable/actionbar_search_selector" />

        <ImageView
            android:id="@+id/moreBtn"
            style="@style/toolbar_menu_style"
            android:layout_alignParentRight="true"
            android:src="@drawable/actionbar_more_selector" />

        <RelativeLayout
            android:id="@+id/tabLayout"
            android:layout_width="wrap_content"
            android:layout_height="fill_parent"
            android:layout_centerInParent="true"
            android:paddingLeft="13.0dip">

            <TextView
                android:id="@+id/left_tab"
                style="@style/toolbar_tab_text"
                android:text="推荐" />

            <TextView
                android:id="@+id/center_tab"
                style="@style/toolbar_tab_text"
                android:layout_toRightOf="@id/left_tab"
                android:text="精选" />

            <TextView
                android:id="@+id/right_tab"
                style="@style/toolbar_tab_text"
                android:layout_toRightOf="@id/center_tab"
                android:visibility="gone"
                android:text="好声音" />
        </RelativeLayout>
    </RelativeLayout>
</RelativeLayout>